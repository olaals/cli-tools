#!/bin/bash

# clip.sh
# Usage: ./clip.sh filename.txt

if [ "$#" -ne 1 ]; then
	echo "Usage: $0 filename.txt"
	exit 1
fi

file_name="$1"

if [ ! -f "$file_name" ]; then
	echo "File $file_name does not exist."
	exit 1
fi

cat "$file_name" | pbcopy
echo "Contents of $file_name copied to clipboard."
